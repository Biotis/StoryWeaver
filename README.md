# StoryWeaver는
AI StoryWeaver는 Google **Gemini 2.5 Flash**를 활용하여  
사용자가 입력한 주제에 맞는 짧은 스토리와 삽화를 동시에 생성하는 간단한 웹 데모입니다.

## 필요 라이브러리 설치
```pip install -r requirements.txt```

## 실행
> uvicorn app:app --reload